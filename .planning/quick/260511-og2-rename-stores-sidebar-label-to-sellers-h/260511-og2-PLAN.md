---
phase: quick-260511-og2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/admin/src/components/layout/main-layout/main-layout.tsx
autonomous: true
requirements:
  - QUICK-260511-OG2
must_haves:
  truths:
    - "Admin sidebar entry that previously read 'חנויות' now reads 'מוכרים'"
    - "The 'מוכרים' sidebar entry still navigates to /stores"
    - "All other sidebar entries remain unchanged"
  artifacts:
    - path: "packages/admin/src/components/layout/main-layout/main-layout.tsx"
      provides: "useCoreRoutes() with hardcoded Hebrew label 'מוכרים' for the /stores entry"
      contains: "מוכרים"
  key_links:
    - from: "useCoreRoutes() Stores entry"
      to: "/stores route"
      via: "to: '/stores' with label: 'מוכרים'"
      pattern: "label: \"מוכרים\""
---

<objective>
Rename the admin sidebar "Stores" entry from the i18n-translated label `t("stores.domain")` (currently rendering as "חנויות") to a hardcoded Hebrew string `"מוכרים"` so that vendor management is discoverable in the admin sidebar. Keep the route target `/stores` and the `BuildingStorefront` icon unchanged.

Purpose: Users do not associate "חנויות" with vendor management. Renaming to "מוכרים" (sellers) makes the entry discoverable without changing the underlying route or i18n keys elsewhere.

Output: Single-line edit to `useCoreRoutes()` swapping the `label` value for the `/stores` entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@packages/admin/src/components/layout/main-layout/main-layout.tsx

<interfaces>
<!-- Current Stores entry in useCoreRoutes() at packages/admin/src/components/layout/main-layout/main-layout.tsx (lines 362-366): -->

```tsx
{
  icon: <BuildingStorefront />,
  label: t("stores.domain"),
  to: "/stores",
},
```

<!-- Target shape after edit: -->

```tsx
{
  icon: <BuildingStorefront />,
  label: "מוכרים",
  to: "/stores",
},
```
</interfaces>

Prior related quick task `260511-vendors-label-redirect` (commit 0410d7b0) is in STATE.md as having renamed "Stores" → "Vendors" and added a `/vendors` redirect. The current file shows the entry has reverted to `t("stores.domain")` — this plan re-applies the rename with a hardcoded Hebrew label (no i18n indirection, no route change).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Stores label with hardcoded Hebrew "מוכרים"</name>
  <files>packages/admin/src/components/layout/main-layout/main-layout.tsx</files>
  <action>In `useCoreRoutes()` locate the object whose `to` is `"/stores"` (currently around lines 362-366). Change ONLY the `label` field from `t("stores.domain")` to the string literal `"מוכרים"`. Do NOT change the `icon` (`<BuildingStorefront />`), do NOT change `to` (`"/stores"`), do NOT change any other entry in the array, do NOT add `items`, and do NOT touch the `useTranslation()` hook call at the top of `useCoreRoutes()` (other entries still use `t(...)`). The label MUST be a plain string literal — no `t()` wrapper, no translation key.</action>
  <verify>
    <automated>grep -n 'label: "מוכרים"' packages/admin/src/components/layout/main-layout/main-layout.tsx | grep -v '^#' | grep -c 'מוכרים' | awk '{ if ($1 == 1) print "ok"; else { print "FAIL count="$1; exit 1 } }' && grep -A1 'label: "מוכרים"' packages/admin/src/components/layout/main-layout/main-layout.tsx | grep -q 'to: "/stores"' && echo "route ok" && ! grep -q 't("stores.domain")' packages/admin/src/components/layout/main-layout/main-layout.tsx && echo "old label removed"</automated>
  </verify>
  <done>The `/stores` sidebar entry in `useCoreRoutes()` uses the hardcoded label `"מוכרים"`. The route still points to `/stores` and the icon is still `<BuildingStorefront />`. The string `t("stores.domain")` no longer appears anywhere in `packages/admin/src/components/layout/main-layout/main-layout.tsx`.</done>
</task>

</tasks>

<verification>
1. `grep -n 'label: "מוכרים"' packages/admin/src/components/layout/main-layout/main-layout.tsx` returns exactly one line.
2. `grep -n 't("stores.domain")' packages/admin/src/components/layout/main-layout/main-layout.tsx` returns zero matches.
3. `grep -A1 'label: "מוכרים"' packages/admin/src/components/layout/main-layout/main-layout.tsx` shows `to: "/stores"` on the next line.
4. No other entries in `useCoreRoutes()` were modified (visual diff inspection).
</verification>

<success_criteria>
- Admin sidebar now renders "מוכרים" where it previously rendered "חנויות"
- Clicking "מוכרים" still navigates to `/stores`
- No regression on other sidebar entries (Orders, Products, Inventory, Customers, Promotions, Price Lists, Payouts)
- No new TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/quick/260511-og2-rename-stores-sidebar-label-to-sellers-h/260511-og2-SUMMARY.md` recording the single-line label change and verification result.
</output>
