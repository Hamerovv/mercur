---
phase: quick-260511-3bg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin/index.html
  - apps/admin/package.json
  - apps/admin/tsconfig.json
  - apps/admin/tsconfig.app.json
  - apps/admin/tsconfig.node.json
  - apps/admin/vite.config.ts
  - apps/admin/src/main.tsx
  - apps/admin/public/bookshook-logo.png
  - Dockerfile
  - apps/api/medusa-config.ts
autonomous: true
requirements:
  - QUICK-260511-3bg
must_haves:
  truths:
    - "apps/admin exists as a standalone Vite app that builds @mercurjs/admin into a static dist/"
    - "Dockerfile builds apps/admin via vite and copies dist/ into runtime image at .medusa/server/admin/dist/"
    - "Medusa admin-ui module resolves appDir to {cwd}/admin so DashboardBase serves /dashboard correctly"
    - "Production container loads /dashboard without showing 'Dashboard not built'"
  artifacts:
    - path: "apps/admin/index.html"
      provides: "Vite HTML shell with #root mount point"
      contains: "/src/main.tsx"
    - path: "apps/admin/src/main.tsx"
      provides: "React bootstrap importing App from @mercurjs/admin"
      contains: "@mercurjs/admin"
    - path: "apps/admin/vite.config.ts"
      provides: "Vite config with base /dashboard/ and mercurDashboardPlugin"
      contains: "/dashboard/"
    - path: "apps/admin/package.json"
      provides: "Workspace package @bookshook/admin depending on @mercurjs/admin"
      contains: "@mercurjs/admin"
  key_links:
    - from: "apps/admin/src/main.tsx"
      to: "@mercurjs/admin"
      via: "named default import of App + side-effect css import"
      pattern: "from ['\"]@mercurjs/admin['\"]"
    - from: "Dockerfile builder stage"
      to: "apps/admin/dist"
      via: "RUN cd apps/admin && bunx vite build"
      pattern: "apps/admin.*vite build"
    - from: "Dockerfile runtime stage"
      to: ".medusa/server/admin/dist/"
      via: "COPY --from=builder /app/apps/admin/dist/ ./admin/dist/"
      pattern: "apps/admin/dist.*admin/dist"
    - from: "apps/api/medusa-config.ts admin-ui module"
      to: "{cwd}/admin"
      via: "appDir: path.join(process.cwd(), 'admin')"
      pattern: "appDir:.*process\\.cwd\\(\\).*admin"
---

<objective>
Create a standalone Vite app at `apps/admin/` that builds `@mercurjs/admin` into a static `dist/`, then wire the Dockerfile and `medusa-config.ts` so the Mercur custom admin is served at `/dashboard` in production instead of the "Dashboard not built" placeholder.

Purpose: `packages/admin` is a tsup component library; nothing builds it into a Vite app. DashboardBase looks for `{cwd}/dist/index.html` (cwd = `/app/apps/api/.medusa/server/`) at runtime, so without a Vite app + a copy step + an `appDir` pointing at it, the dashboard never resolves an HTML entry.

Output: `apps/admin/*` scaffold mirroring `apps/vendor/`, Dockerfile with admin build + copy steps, and `appDir` resolved to `{cwd}/admin`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@apps/vendor/src/main.tsx
@apps/vendor/vite.config.ts
@apps/vendor/package.json
@apps/vendor/index.html
@apps/vendor/tsconfig.json
@apps/vendor/tsconfig.app.json
@apps/vendor/tsconfig.node.json
@Dockerfile
@apps/api/medusa-config.ts
@packages/admin/package.json

<interfaces>
<!-- Key contracts the executor needs. Extracted from @mercurjs/admin exports + apps/vendor pattern. -->

From packages/admin/package.json (exports surface):
- Default import:   `import App from "@mercurjs/admin"` -> React component (the full admin UI)
- CSS side-effect: `import "@mercurjs/admin/index.css"`
- Subpath:          `@mercurjs/admin/pages` (NOT needed for main.tsx)

From apps/vendor/src/main.tsx (the exact template to mirror):
```
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import "@mercurjs/vendor/index.css";
import App from "@mercurjs/vendor";

createRoot(document.getElementById("root")!).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```
For apps/admin/src/main.tsx: swap `@mercurjs/vendor` -> `@mercurjs/admin`. Identical otherwise.

From apps/vendor/vite.config.ts (mercurDashboardPlugin signature):
- `medusaConfigPath: '../../apps/api/medusa-config.ts'` (relative from apps/admin -> apps/api -> same path works)
- `name`, `logo`, `components` are app-level branding/component overrides
- For admin: ADD `base: '/dashboard/'` at the top level of defineConfig — required so built asset URLs are prefixed to match the route DashboardBase mounts at.

From Dockerfile (current builder stage layout):
- Builder runs `cd apps/api && bunx medusa build` at line ~40
- Builder DOES NOT currently install apps/vendor or apps/admin dependencies — `bun install` at line 26 installs the whole workspace, so apps/admin will be installed automatically once its package.json exists AND its package.json is COPYed before `bun install`
- IMPORTANT: per Dockerfile lines 10-21, every workspace package.json is COPIED before `bun install`. apps/admin/package.json MUST be added to that copy list.

From apps/api/medusa-config.ts (current admin-ui module entry):
```
{ resolve: '@mercurjs/core/modules/admin-ui',
  options: { appDir: '', path: '/dashboard', disable: false } }
```
- `path: '/dashboard'` is correct, leave alone
- `appDir: ''` must change to `path.join(process.cwd(), 'admin')` -> resolves at runtime to `/app/apps/api/.medusa/server/admin`
- Add `import path from 'path'` at top of medusa-config.ts (NOT already imported)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold apps/admin Vite app mirroring apps/vendor</name>
  <files>apps/admin/index.html, apps/admin/package.json, apps/admin/tsconfig.json, apps/admin/tsconfig.app.json, apps/admin/tsconfig.node.json, apps/admin/vite.config.ts, apps/admin/src/main.tsx, apps/admin/public/bookshook-logo.png</files>
  <action>
Create apps/admin/ as a workspace package mirroring apps/vendor/ structure with these required deltas:

1. apps/admin/package.json:
   - name: "@bookshook/admin", private: true, version: "0.0.0", type: "module"
   - scripts: dev "vite --port 7002", lint "eslint .", preview "vite preview --port 7002", build "vite build"
   - dependencies: same set as apps/vendor/package.json EXCEPT replace `"@mercurjs/vendor": "*"` with `"@mercurjs/admin": "*"`. Keep all other deps identical (react 18.3.1, react-dom 18.3.1, zod 3.25.76, @medusajs/icons ^2.13.4, @medusajs/ui 4.1.1, @mercurjs/dashboard-sdk "*", @mercurjs/dashboard-shared "*", @mercurjs/client "*", react-i18next 13.5.0, @tanstack/react-query 5.64.2, react-router-dom 6.30.3, @tanstack/react-table 8.20.5, react-hook-form 7.49.1, @hookform/resolvers 3.4.2, jsonwebtoken 9.0.2, react-jwt ^2.0.0, radix-ui 1.1.2)
   - devDependencies: copy apps/vendor/package.json devDependencies verbatim (vite ^5.4.21, @vitejs/plugin-react ^4.2.1, typescript 5.9.3, @types/react, @types/react-dom, @types/jsonwebtoken, eslint config, @mercurjs/types workspace:*, etc.)

2. apps/admin/src/main.tsx — mirror apps/vendor/src/main.tsx exactly, swap `@mercurjs/vendor` -> `@mercurjs/admin` in both the css import and the default App import. Keep StrictMode + createRoot.

3. apps/admin/vite.config.ts — copy apps/vendor/vite.config.ts and:
   - Add `base: '/dashboard/'` at the top of defineConfig (sibling to plugins). This makes built asset URLs (`/dashboard/assets/*.js`) match where DashboardBase mounts the SPA.
   - Inside mercurDashboardPlugin(...): keep `medusaConfigPath: '../../apps/api/medusa-config.ts'`, change `name` to `'בוקשוק - ניהול'` (admin panel branding), keep `logo: '/bookshook-logo.png'`, REMOVE the `components: { StoreSetup: ... }` override (vendor-specific) — admin does not need a StoreSetup component swap. If unsure, copy the components block 1:1 from vendor; harmless if unused.

4. apps/admin/index.html — copy apps/vendor/index.html, change `<title>` to `בוקשוק - ניהול`. Keep `lang="he" dir="rtl"`, keep `<link rel="icon" ... bookshook-logo.png">`, keep `<div id="root">`, keep `<script type="module" src="/src/main.tsx">`.

5. apps/admin/public/bookshook-logo.png — copy from apps/vendor/public/bookshook-logo.png (or `cp apps/vendor/public/bookshook-logo.png apps/admin/public/`). Required because index.html references it.

6. apps/admin/tsconfig.json, tsconfig.app.json, tsconfig.node.json — copy verbatim from apps/vendor/. No path changes needed (relative `./src` is identical).

Do NOT add apps/admin to the root `workspaces` field — package.json already includes `"apps/*"` so the workspace is picked up automatically.
Do NOT touch packages/admin/ — that is the tsup library; this plan consumes it, not modifies it.
  </action>
  <verify>
    <automated>cd /Users/hadashamerovv/Documents/Coding/bookshook-marketplace && test -f apps/admin/index.html && test -f apps/admin/src/main.tsx && test -f apps/admin/vite.config.ts && test -f apps/admin/package.json && test -f apps/admin/public/bookshook-logo.png && grep -q '"@mercurjs/admin"' apps/admin/package.json && grep -q "@mercurjs/admin" apps/admin/src/main.tsx && grep -q "base: '/dashboard/'" apps/admin/vite.config.ts && echo "OK"</automated>
  </verify>
  <done>apps/admin/ contains index.html, package.json, vite.config.ts, src/main.tsx, public/bookshook-logo.png, and tsconfig.{,app,node}.json. main.tsx imports App + index.css from @mercurjs/admin. vite.config.ts sets base '/dashboard/' and uses mercurDashboardPlugin. package.json declares @bookshook/admin with @mercurjs/admin workspace dep. `bun install` from repo root completes without error and links @bookshook/admin into the workspace.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Dockerfile to build admin and copy dist into runtime</name>
  <files>Dockerfile</files>
  <action>
Update /Users/hadashamerovv/Documents/Coding/bookshook-marketplace/Dockerfile with three edits:

EDIT 1 — Builder stage manifest copy list (after the existing `COPY apps/api/package.json ./apps/api/` line near line 11):
Add: `COPY apps/admin/package.json ./apps/admin/`
This must be inserted BEFORE the `bun install` at line 26 so the workspace resolver sees apps/admin during install.

EDIT 2 — Builder stage source copy (after the existing `COPY apps/api/ ./apps/api/` and `COPY packages/ ./packages/` lines near line 29-30):
Add: `COPY apps/admin/ ./apps/admin/`
This brings the admin source (vite.config.ts, src/, index.html, public/) into the builder.

EDIT 3 — Builder stage admin build (insert AFTER `RUN cd apps/api && bunx medusa build` at line 40, BEFORE the `RUN rm -f .npmrc` line at line 43):
```
# Build the custom Mercur admin Vite app (produces apps/admin/dist/)
RUN cd apps/admin && bunx vite build
```
Reason for ordering: medusa build sets up `.medusa/server/`; the admin build is independent but kept after for layer-cache stability.

EDIT 4 — Runtime stage copy (insert AFTER the existing `COPY --from=builder /app/apps/api/.medusa/server/ ./` line at line 62, BEFORE `EXPOSE 9000`):
```
# Copy the custom admin SPA into the runtime so DashboardBase can serve /dashboard
COPY --from=builder /app/apps/admin/dist/ ./admin/dist/
```
Working dir is `/app/apps/api/.medusa/server` (set at line 50), so this resolves to `/app/apps/api/.medusa/server/admin/dist/index.html` — which is exactly where the updated `appDir` (Task 3) will point.

Do NOT change the `WORKDIR`, the `CMD`, the `npm install --omit=dev` step, or the cache-friendly package.json+lockfile copy at lines 55-57.
Do NOT add a separate `bun install` in apps/admin — the workspace install at line 26 covers it.
Do NOT change to `npm run build` — use `bunx vite build` directly (matches builder image).
  </action>
  <verify>
    <automated>cd /Users/hadashamerovv/Documents/Coding/bookshook-marketplace && grep -q 'COPY apps/admin/package.json ./apps/admin/' Dockerfile && grep -q 'COPY apps/admin/ ./apps/admin/' Dockerfile && grep -q 'cd apps/admin && bunx vite build' Dockerfile && grep -q 'COPY --from=builder /app/apps/admin/dist/ ./admin/dist/' Dockerfile && echo "OK"</automated>
  </verify>
  <done>Dockerfile builder stage installs apps/admin via the workspace, runs `bunx vite build` in apps/admin, and runtime stage copies the resulting dist into `.medusa/server/admin/dist/`. `docker compose build medusa` completes without error and the resulting image contains `/app/apps/api/.medusa/server/admin/dist/index.html`.</done>
</task>

<task type="auto">
  <name>Task 3: Point admin-ui module appDir at {cwd}/admin</name>
  <files>apps/api/medusa-config.ts</files>
  <action>
Update /Users/hadashamerovv/Documents/Coding/bookshook-marketplace/apps/api/medusa-config.ts with two edits:

EDIT 1 — Add `import path from 'path'` near the top of the file (after the existing `import { withMercur } from '@mercurjs/core'` line at line 2). The current file does not import path.

EDIT 2 — In the modules array, locate the entry with `resolve: '@mercurjs/core/modules/admin-ui'` (lines 30-37). Change `appDir: ''` to `appDir: path.join(process.cwd(), 'admin')`. Keep `path: '/dashboard'` and `disable: false` unchanged.

Rationale: At runtime CWD is `/app/apps/api/.medusa/server/` (Dockerfile WORKDIR at line 50). `path.join(process.cwd(), 'admin')` resolves to `/app/apps/api/.medusa/server/admin`, and DashboardBase appends `/dist/index.html` -> matches the copy target from Task 2.

Do NOT change the vendor-ui module entry (lines 38-46) — that is a separate concern (apps/vendor served at /seller). Leave its `appDir: ''` alone unless a follow-up plan addresses vendor packaging.
Do NOT add path resolution to vendor-ui in this plan — out of scope.
  </action>
  <verify>
    <automated>cd /Users/hadashamerovv/Documents/Coding/bookshook-marketplace && grep -q "^import path from 'path'" apps/api/medusa-config.ts && grep -q "appDir: path.join(process.cwd(), 'admin')" apps/api/medusa-config.ts && ! grep -A2 "admin-ui" apps/api/medusa-config.ts | grep -q "appDir: ''" && echo "OK"</automated>
  </verify>
  <done>medusa-config.ts imports `path`, the admin-ui module's `appDir` resolves to `{cwd}/admin`, vendor-ui module is unchanged. A fresh `docker compose up medusa` serves the Mercur custom admin at http://localhost:9000/dashboard (no "Dashboard not built" placeholder) and the page loads `/dashboard/assets/*.js` successfully.</done>
</task>

</tasks>

<verification>
End-to-end manual verification after all three tasks complete:

1. From repo root: `bun install` succeeds and links @bookshook/admin into node_modules
2. From repo root: `cd apps/admin && bunx vite build` produces apps/admin/dist/index.html with `<script src="/dashboard/assets/...">` tags
3. `docker compose build --no-cache medusa` completes; resulting image contains `/app/apps/api/.medusa/server/admin/dist/index.html`
4. `docker compose up medusa` boots without `appDir`-related errors in logs
5. `curl -sI http://localhost:9000/dashboard/` returns 200 (not 404, not the "Dashboard not built" HTML)
6. Browser at http://localhost:9000/dashboard shows the Mercur admin login screen (NOT a placeholder page)
7. Browser devtools Network tab shows `/dashboard/assets/*.js` and `/dashboard/assets/*.css` load with 200
</verification>

<success_criteria>
- apps/admin/ exists as a buildable Vite workspace package consuming @mercurjs/admin
- Dockerfile produces an image where /app/apps/api/.medusa/server/admin/dist/index.html exists
- medusa-config.ts resolves admin-ui appDir to `{cwd}/admin` via path.join
- /dashboard in the deployed container renders the Mercur admin UI, not "Dashboard not built"
- No changes to packages/admin/, apps/vendor/, or the vendor-ui module
</success_criteria>

<output>
After completion, create `.planning/quick/260511-3bg-create-apps-admin-vite-app-to-serve-the-/260511-3bg-SUMMARY.md` summarizing files created/modified, the appDir resolution chain, and any deviations from this plan.
</output>
