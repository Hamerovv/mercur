---
phase: quick-260511-v1k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/medusa-config.ts
autonomous: false
requirements:
  - QUICK-260511-v1k
must_haves:
  truths:
    - "Standard Medusa admin at /app/ is disabled (no longer served)"
    - "Custom Mercur admin at /dashboard/ still loads and displays the Hebrew sellers list (מוכרים)"
    - "/dashboard/vendors route resolves (no 404) because the custom admin serves it"
  artifacts:
    - path: "apps/api/medusa-config.ts"
      provides: "Medusa config with top-level admin.disable=true; admin-ui module unchanged at /dashboard"
      contains: "disable: true"
  key_links:
    - from: "apps/api/medusa-config.ts (top-level admin block)"
      to: "Medusa framework admin server"
      via: "admin.disable flag"
      pattern: "admin:\\s*\\{[^}]*disable:\\s*true"
    - from: "apps/api/medusa-config.ts (modules[0] admin-ui)"
      to: "Custom Mercur admin served at /dashboard"
      via: "@mercurjs/core/modules/admin-ui with appDir=admin, path=/dashboard, disable=false"
      pattern: "@mercurjs/core/modules/admin-ui"
---

<objective>
Disable the standard Medusa admin (served at /app/) so it stops shadowing the custom Mercur admin and so users always land on the Hebrew sellers/vendors UI mounted at /dashboard/.

Purpose: Fixes two reported bugs — (1) /app/ shows generic Medusa admin instead of the custom Mercur "מוכרים" UI, (2) /vendors returns 404 because standard Medusa admin has no /vendors route while the custom admin does.

Output: apps/api/medusa-config.ts with top-level `admin.disable: true`. Custom admin-ui module at /dashboard remains untouched (disable: false).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260511-v1k-disable-standard-medusa-admin/

<interfaces>
Current apps/api/medusa-config.ts shape (verified):

```typescript
module.exports = withMercur({
  admin: {
    disable: false,                                        // <-- flip to true
    backendUrl: process.env.MEDUSA_BACKEND_URL || "http://localhost:9000",
  },
  projectConfig: { ... },
  featureFlags: { seller_registration: true },
  modules: [
    {
      resolve: '@mercurjs/core/modules/admin-ui',          // <-- KEEP enabled
      options: {
        appDir: path.join(process.cwd(), 'admin'),
        path: '/dashboard',
        disable: false
      }
    },
    {
      resolve: '@mercurjs/core/modules/vendor-ui',
      options: { appDir: '', path: '/seller', disable: false }
    },
  ],
})
```

Two distinct admin surfaces exist:
- Top-level `admin:` block → MedusaJS built-in admin (served at /app/). DISABLE THIS.
- `modules[0]` `@mercurjs/core/modules/admin-ui` → custom Mercur admin (served at /dashboard/). LEAVE ENABLED.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set top-level admin.disable to true in medusa-config.ts</name>
  <files>apps/api/medusa-config.ts</files>
  <action>In apps/api/medusa-config.ts, change ONLY the top-level admin block. On the line `disable: false,` (currently line 9, directly under `admin: {`), set it to `disable: true,`. Do NOT modify the `disable: false` inside `modules[0]` (the `@mercurjs/core/modules/admin-ui` options block) — that module serves the custom Mercur admin at /dashboard and must stay enabled. Do not touch `backendUrl`, `projectConfig`, `featureFlags`, `modules[1]` (vendor-ui), or any other field. After edit, the file must still parse as valid TypeScript and `withMercur({...})` must still be the module export.</action>
  <verify>
    <automated>cd apps/api && grep -nE 'disable:\s*(true|false)' medusa-config.ts | grep -v '^#' | tee /dev/stderr | awk -F: '{print $1":"$2":"$3}' | (read a; read b; read c; [ "$(echo "$a" | grep -c 'true')" = "1" ] &amp;&amp; [ "$(echo "$b" | grep -c 'false')" = "1" ] &amp;&amp; [ "$(echo "$c" | grep -c 'false')" = "1" ])</automated>
  </verify>
  <done>medusa-config.ts has exactly three `disable:` lines in order: (1) top-level admin → `true`, (2) admin-ui module → `false`, (3) vendor-ui module → `false`. No other lines changed.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild and restart the medusa container</name>
  <files></files>
  <action>From the repo root, rebuild the medusa service image with the updated config and bring it back up. Run: `docker compose build medusa` then `docker compose up -d medusa`. Do not use `--no-cache` (unnecessary — only one file changed) and do not run `down -v` (preserves DB/seed data). Wait for the medusa service to report healthy, then capture the last ~50 lines of `docker compose logs medusa` to confirm startup finished without errors. If a Docker auth/registry error blocks the build, create a checkpoint:human-action and stop — do not retry blindly.</action>
  <verify>
    <automated>docker compose ps medusa --format json | grep -q '"State":"running"' &amp;&amp; curl -sf -o /dev/null -w '%{http_code}\n' http://localhost:9000/dashboard | grep -qE '^(200|301|302)$'</automated>
  </verify>
  <done>medusa container is running; GET http://localhost:9000/dashboard returns 2xx/3xx; container logs show no fatal startup errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify both bugs are fixed</name>
  <what-built>Standard Medusa admin disabled (admin.disable=true). Custom Mercur admin at /dashboard remains enabled. medusa container rebuilt and restarted.</what-built>
  <how-to-verify>
    1. Open http://localhost:9000/app/ in a fresh browser tab (or incognito). EXPECTED: 404 / redirect / blank — the standard Medusa admin should NOT load. If you still see the generic Medusa admin UI, the change did not take effect.
    2. Open http://localhost:9000/dashboard/ in the same browser. EXPECTED: custom Mercur admin loads; sidebar shows the Hebrew label "מוכרים" (Sellers/Vendors).
    3. Click "מוכרים" (or navigate to http://localhost:9000/dashboard/vendors). EXPECTED: vendors list renders (NOT a 404).
    4. If any of (1)-(3) fail, report which step + the actual screen/HTTP response. Otherwise reply "approved".
  </how-to-verify>
  <resume-signal>Type "approved" or describe the failing step</resume-signal>
</task>

</tasks>

<verification>
- Top-level `admin.disable` is `true` in apps/api/medusa-config.ts.
- `@mercurjs/core/modules/admin-ui` module still has `disable: false` and `path: '/dashboard'`.
- medusa container restarted cleanly with the new config.
- /app/ no longer serves the standard Medusa admin.
- /dashboard/ serves the custom Mercur admin with the Hebrew "מוכרים" sidebar label.
- /dashboard/vendors resolves (no 404).
</verification>

<success_criteria>
- Bug 1 fixed: /app/ no longer shows generic Medusa admin UI.
- Bug 2 fixed: /dashboard/vendors loads the vendors list (no 404).
- No regression: /dashboard/ still loads the custom Mercur admin with Hebrew sellers label.
- Only apps/api/medusa-config.ts modified; only the top-level `admin.disable` flipped.
</success_criteria>

<output>
After completion, create `.planning/quick/260511-v1k-disable-standard-medusa-admin/260511-v1k-SUMMARY.md` describing the one-line change, rebuild command run, and verification results.
</output>
